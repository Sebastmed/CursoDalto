/*
METODOS DE CADENA       variable.propiedad()

concat()                Junta dos o más cadenas y retorna una nueva.

startsWith()            Si una cadena comienza con los caracteres de otra cadena, devuelve true, sino devuelve false.
endsWith()              Si una cadena termina con los caracteres de otra cadena, devuelve true, sino devuelve false.
includes()              Si una cadena puede encontrarse dentro de otra cadena, devuelve true, sino devuelve false.
indexOf()               Devuelve el índice del primer caracter de la cadena, si hay coincidencia, sino -1.
lastIndexOf()           devuelve el último índice del primer caracter de la cadena si hay coincidencia, sino -1.
Number.isInteger()      Determina si el valor es entero, si lo es devulev true, sino devuelve false.

padStart(8,"")          [propuesta de Esandar] - Rellenar cadena al principio con los caracteres deseados.
padEnd(10,"")           [propuesta de ECMA] - Rellenar cadena al final con los caracteres deseados.
repeat(10)              Devuelve la misma cadena pero repetida la cantidad que le digamos.

split()                 Divide la cadena como le pedimos (arrays).
substring()             Nos retorna un pedazo de la cadena que seleccionamos.
toLowerCase()           Convierte una cadena a minúscula.
toUpperCase()           Convierte una cadena a mayúscula.
toString()              Método que devuelve una cadena que representa al objeto.
trim()                  Elimina los espacios en blanco al principio y al final de una cadena.
trimEnd()               Elimina los espacion en blanco al final de una cadena.
trimStart()             Elimina los espacion en blanco al principio de una cadena.
valuOf()                Retorna el valor primitivo de un objeto string.


METODOS DE ARRAYS

Transformadores:

pop()               Elimina el último elemento de un array y lo devuelve.
shift()             Elimina el primer elemento de una array y lo devuelve.
push()              Agrega un elemento al array al final de la lista.
unshift()           Agrega uno o más elementos al inicio del array, y devuelve la nueva longitud del array.
reverse()           Invierte el orden de los elementos de una array.
sort()              Ordena los elementos de un array localmente y devuelve el arreglo ordenado.
splice()            Cambia el contenido de un array eliminando elementos existentes y/o agregando nuevos elementos.

Accesores:

join()              Une todos los elementos de una matriz (u objeto similar) en una cadena y la devuelve.
slice()             Devuelve una parte del array dentro de un nuevo array empezando por inicio hasta el fin (fin no incluido).
toString()          MÉTODOS DE CADENA
includes()          MÉTODOS DE CADENA
indexOf()           MÉTODOS DE CADENA
lastIndexOf()                            MÉTODOS DE CADENA

Repetición:

filter()                    Crea un nuevo array con todos los elementos que cumplan la condición.
forEach()                   Ejecuta la función indicada una vez por cada elemento del array.

Otras:
arrays.at()                 Nos da el elemento dentro del array, si usamos negativos va del último al primero.
array.length                Nos dice cúantos elementos tiene el array.
array[array.lenght-1]       Nos da el último elemento del array.


OBJETO MATH - BÁSICO

Métodos:            Math.

sqrt()              Devuelve la raíz cuadrada de un número positivo.
cbrt()              Devuelve la raíz cúbica de un número.
max()               Devuelve el mayor de cero o más números.
min()               Devuelve el más pequeño de cero o más números.
random()            Devuelve un número pseudo-aleatorio entre 0 y 1.
round()             Devuelve el valor de un número redondeado al número más cercano.
fround()            Devuelve la representación flotante de presición simple más cercana de un número.
floor()             Devuelve el mayor entero menor que o igual a un número
trunc()             Devuelve la parte entera del número x, la eliminación de los digitos fraccionarios.

Propiedades:        Math.

PI                  Ratio de la circunferencia de un círculo respecto a su diámetro, aproximadamente 3.14159.
SQRT1_2             Raíz cuadrada de 1/2; equivalente, 1 sobre la raíz cuadrada de 2, aproximadamente 0.707.
SQRT2               Raíz cuadrada de 2, aproximadamente 1.414.

E                   Constante de Euler, la base de los logarímos naturales, aproximadamente 2.718.
LN2                 Logarítmo natural de 2, aproximadamente 0.663.
LN10                Logarítmo natural de E, aproximadamente 2.303.
LOG2E               Logarítmo de E con base 2, aproximadamente 1.443.
LOG10E              Logaritmo de E con base 10, aproximadamente 0.434.




*/

